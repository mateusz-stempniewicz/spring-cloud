spring:
  application:
    name: microservice-users
    
---
spring:
  profiles: development
server:
  address: 127.0.0.1
  port: ${PORT:0}
  

---
spring:
  profiles: production
server:
  address: 127.0.0.5
  port: ${PORT:10000}